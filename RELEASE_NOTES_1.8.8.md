# Tamfis Code 1.8.8

- Added `! <command>` as a direct shell-command trigger in the interactive session.
- Commands such as `! ssh user@example.com` use the existing shell execution, workspace, approval, and safety controls.
- Kept `$ <command>`, `/run <command>`, and `/shell <command>` as supported aliases.
