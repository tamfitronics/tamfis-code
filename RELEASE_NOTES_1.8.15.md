# Tamfis Code 1.8.15

- Require bounded repository-tree orientation before broad code searches, then support focused multi-file searches and reads.
- Promote explicit fix requests to tool-capable debug execution even when heuristic routing initially misclassifies the task.
- Reject advice-only completion for implementation requests unless there is verified mutation or evidence-backed no-change proof.
- Synthesize concise, evidence-based conversation recaps instead of copying typo-heavy prompts or speculative model reasoning.
- Make offline typo correction intent-safe with reviewed repairs and protection for valid words, literals, paths, flags, versions, and negation.
- Normalize unsupported `git show --no-stat` calls and add targeted diagnostics for Git, test, syntax, dependency, authentication, network, and rate-limit failures.
- Add local `glob_files` and guarded `web_fetch` tools with policy, safety, and standalone UI integration.
- Rotate fallback attempts across each configured provider's model pool, retain a bounded 600-second retry anchor even when NVIDIA NIM is absent, and allow operators to tune long-output continuation rounds.
