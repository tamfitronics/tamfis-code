# Tamfis Code 1.8.14

- Accept `limit` as an alias for `search_code.max_results`, preventing provider compatibility failures from unexpected keyword arguments.
- Preserve existing pagination and result caps for both parameter names.
