# Tamfis-Code 1.7.76

## Repository validation discovery

- Shell validation is now discovered recursively, including repositories
  whose scripts live under `scripts/`, while dependency and build trees remain
  excluded.
